import React from 'react';
import PropTypes from 'prop-types';
import { VotingPopupWrapper } from './VotingPopup.styled';

const VotingPopup = () => (
 <VotingPopupWrapper>
    VotingPopup Component
 </VotingPopupWrapper>
);

VotingPopup.propTypes = {};

VotingPopup.defaultProps = {};

export default VotingPopup;
