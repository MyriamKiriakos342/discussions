import React from 'react';
import PropTypes from 'prop-types';
import { MenuWrapper } from './Menu.styled';

const Menu = () => (
 <MenuWrapper>
    Menu Component
 </MenuWrapper>
);

Menu.propTypes = {};

Menu.defaultProps = {};

export default Menu;
